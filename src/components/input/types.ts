export interface IInputThemes {
  base: string;
  size: {
    fullWidth: string;
    sm: string;
    md: string;
    base: string;
  };
  variant: {
    primary: string;
    outlined: string;
    disabled: string;
  };
}
